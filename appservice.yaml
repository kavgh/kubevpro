apiVersion: v1
kind: Service
metadata:
  name: vproappsvc
spec:
  selector:
    matchLabels:
      app: vproapp
  ports:
  - port: 8080
    targetPort: vproapp-port
    protocol: TCP
  type: ClusterIp