apiVersion: v1
kind: PersistentVolumClaim
metadata:
  name: db-pv-claim
  labels:
    app: vprodb
spec:
  accessMode:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: default